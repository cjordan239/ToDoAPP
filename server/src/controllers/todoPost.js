const { createTodoQueries } = require("../Queries/todoQueries")

const createTodoPost = async (req, res) => {
    try {
        const {title, content, categories} = req.body 
        const user_id = req.session
    } catch {

    }
}